interface:
  display_name: "Extract Figure Data"
  short_description: "Extract discrete chart points and table values."
  default_prompt: "Use $extract-from-figures to extract visible discrete values from this cataloged MinerU paper folder."

