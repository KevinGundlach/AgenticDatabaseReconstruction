interface:
  display_name: "Extract Series"
  short_description: "Visually extract PDF tables and plotted series to JSON."
  default_prompt: "Use $extract-series to visually read this PDF and extract the selected tables or discrete plotted series into validated JSON."
