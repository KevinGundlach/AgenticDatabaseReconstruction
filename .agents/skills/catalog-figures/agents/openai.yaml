interface:
  display_name: "Catalog Figures"
  short_description: "Catalog visible data fields in charts and tables."
  default_prompt: "Use $catalog-figures to catalog the visible data fields in every chart and table in this MinerU paper folder."
