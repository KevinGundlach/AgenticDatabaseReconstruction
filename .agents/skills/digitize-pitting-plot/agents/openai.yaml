interface:
  display_name: "Digitize Pitting Plot"
  short_description: "Digitize one pitting-potential plot to JSON"
  default_prompt: "Use $digitize-pitting-plot to digitize one staged pitting-potential plot and validate its JSON output."
policy:
  allow_implicit_invocation: true

