interface:
  display_name: "Parse Corrosion Tables"
  short_description: "Parse corrosion tables into structured JSON"
  default_prompt: "Use $parse-corrosion-tables to parse staged corrosion table images into structured JSON."
